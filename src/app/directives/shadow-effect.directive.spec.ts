import { ShadowEffectDirective } from './shadow-effect.directive';

describe('ShadowEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowEffectDirective();
    expect(directive).toBeTruthy();
  });
});
